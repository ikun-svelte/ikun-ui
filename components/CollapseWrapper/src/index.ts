/// <reference types="./types" />
import CollapseWrapper from './index.svelte';
export { CollapseWrapper as KCollapseWrapper };

export default CollapseWrapper;

export type { KCollapseWrapperProps, collapseMapType } from './types';
